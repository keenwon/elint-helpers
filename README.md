# ELint Helpers

[elint](https://github.com/x-orpheus/elint) preset helper

## 使用方式

在 preset 包内安装依赖

```shell
npm i elint-helpers
```

添加 `postinstall`

```json
{
  "scripts": {
    "postinstall": "elint-helpers install"
  }
}
```
